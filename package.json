{
  "name": "mfuns-m7engine",
  "version": "1.0.0",
  "description": "基于TS的M7高级弹幕引擎",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --mode=production && tsc",
    "watch": "webpack --watch --mode=development",
    "lint:fix": "tslint -c tslint.json 'src/**/*.ts' --fix"
  },
  "author": "Minteea",
  "license": "MIT",
  "dependencies": {
    "webpack": "^5.73.0"
  },
  "devDependencies": {
    "ts-loader": "^9.3.1",
    "webpack-cli": "^4.10.0"
  }
}
